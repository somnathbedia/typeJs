"use strict";
let evenNumbers = [12, 14, 16, 18, 20, 22];
let printNumbers = (array) => {
    for (let i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
};
console.log(evenNumbers);
printNumbers(evenNumbers);
